const CONFIG={fastInterval:1500,slowInterval:12000};const nativeUI={};let previousFinishedPids=null;let previousVideoKeys=null;let previousMusicKeys=null;let lastActiveState=false;let originalDocTitle=null;let notifyEnabled=false;let notifyToggleBtn=null;const NOTIFY_KEY='yt_notify_enabled';const BG_POLL_INTERVAL=5000;function escapeHtml(value){return String(value??"").replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;").replace(/'/g,"&#39;");}
function safeUrlAttr(url){const trimmed=String(url??"").trim();if(!/^https?:\/\//i.test(trimmed))return"#";return escapeHtml(trimmed);}
function safePid(pid){const str=String(pid??"");return/^[A-Za-z0-9_-]+$/.test(str)?str:"";}
let audioSuccess=null;let audioError=null;let soundsLoading=null;function getCsrfToken(){const meta=document.querySelector('meta[name="csrf-token"]');return meta?meta.getAttribute('content'):'';}
function confirmAction(action,value,extraFields={}){const messages={'kill':value==='all'?'Остановить ВСЕ загрузки?':'Остановить загрузку?','delete':'Удалить файл безвозвратно?','clear':value==='recent'?'Очистить историю загрузок?':(value==='queue'?'Очистить очередь?':'Удалить из истории?'),'restart':'Перезапустить загрузку?','removeQueued':'Удалить из очереди?'};if(!confirm(messages[action]||'Выполнить действие?')){return;}
const form=document.createElement('form');form.method='POST';form.action='index.php';form.style.display='none';const addField=(name,val)=>{const input=document.createElement('input');input.type='hidden';input.name=name;input.value=val;form.appendChild(input);};addField('csrf_token',getCsrfToken());addField(action,value);for(const[key,val]of Object.entries(extraFields)){addField(key,val);}
document.body.appendChild(form);form.submit();form.remove();}
function updateFileBadges(data){const videoBadge=document.getElementById('video-badge');const musicBadge=document.getElementById('music-badge');if(videoBadge){if(data.videos&&data.videos.length>0){videoBadge.classList.add('is-visible');}else{videoBadge.classList.remove('is-visible');}}
if(musicBadge){if(data.music&&data.music.length>0){musicBadge.classList.add('is-visible');}else{musicBadge.classList.remove('is-visible');}}}
function updateProxyStatus(proxy){const box=document.getElementById('proxy-status');if(!box||!proxy||!proxy.enabled)return;if(proxy.unset){box.setAttribute('data-state','unset');return;}
const dotClass=v=>{if(v===null||v===undefined)return'is-pending';if(v==='warn')return'is-warn';return v==='death'?'is-death':'is-work';};const windows=proxy.windows||{};box.querySelectorAll('.proxy-dot').forEach(dot=>{const win=dot.getAttribute('data-win');dot.classList.remove('is-work','is-warn','is-death','is-pending');dot.classList.add(dotClass(windows[win]));});box.setAttribute('data-state',proxy.state||'pending');}
async function preloadNotificationSounds(){if(audioSuccess&&audioError)return;if(soundsLoading)return soundsLoading;soundsLoading=(async()=>{try{const[successResp,errorResp]=await Promise.all([fetch('finish_job.mp3'),fetch('error_job.mp3')]);const[successBlob,errorBlob]=await Promise.all([successResp.blob(),errorResp.blob()]);audioSuccess=new Audio(URL.createObjectURL(successBlob));audioSuccess.volume=0.5;audioError=new Audio(URL.createObjectURL(errorBlob));audioError.volume=0.5;}catch(error){console.warn("Не удалось предзагрузить звуки через Blob, используется fallback:",error);audioSuccess=new Audio('finish_job.mp3');audioSuccess.volume=0.5;audioError=new Audio('error_job.mp3');audioError.volume=0.5;}finally{soundsLoading=null;}})();return soundsLoading;}
function unloadNotificationSounds(){if(audioSuccess){if(audioSuccess.src.startsWith('blob:'))URL.revokeObjectURL(audioSuccess.src);audioSuccess.pause();}
if(audioError){if(audioError.src.startsWith('blob:'))URL.revokeObjectURL(audioError.src);audioError.pause();}
audioSuccess=null;audioError=null;}
function isDownloadFailed(item){const status=item.status||"";const type=item.type||"";if(status.includes("Отменено")||status.includes("Ошибка")||status.includes("Порнографию")||status.includes("не та ссылка")){return true;}
if(type==="unknown"){return true;}
return false;}
const isMobileDevice=/Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent)||(window.innerWidth<=768&&'ontouchstart'in window);const notificationsSupported=!isMobileDevice&&('Notification'in window);let isSoundMuted=localStorage.getItem('yt_sound_muted')==='true';if(!isMobileDevice&&!isSoundMuted){if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',preloadNotificationSounds);}else{preloadNotificationSounds();}}
function isOnHomePage(){const hash=window.location.hash.toLowerCase();return hash===''||hash==='#home'||hash==='#';}
let soundToggleBtn=null;function updateButtonVisibility(){if(!soundToggleBtn)return;soundToggleBtn.classList.toggle('is-visible',isOnHomePage());}
function initSoundToggle(){if(isMobileDevice)return;if(document.getElementById('sound-toggle')){soundToggleBtn=document.getElementById('sound-toggle');updateButtonVisibility();return;}
const btn=document.createElement('div');btn.id='sound-toggle';btn.className='sound-toggle';soundToggleBtn=btn;updateSoundButtonVisuals(btn);updateButtonVisibility();btn.addEventListener('click',()=>{isSoundMuted=!isSoundMuted;localStorage.setItem('yt_sound_muted',isSoundMuted);updateSoundButtonVisuals(btn);if(!isSoundMuted){preloadNotificationSounds();}else{unloadNotificationSounds();}});document.body.appendChild(btn);}
function updateSoundButtonVisuals(btn){btn.innerHTML=isSoundMuted?'🔇':'🔊';btn.title=isSoundMuted?'Звук выключен. Нажмите, чтобы включить.':'Звук включен. Нажмите, чтобы выключить.';}
if(!isMobileDevice){if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',()=>{initSoundToggle();window.addEventListener('hashchange',updateButtonVisibility);});}else{initSoundToggle();window.addEventListener('hashchange',updateButtonVisibility);}}
function playNotificationSound(isSuccess){if(isMobileDevice||isSoundMuted)return;if(!audioSuccess||!audioError){preloadNotificationSounds().then(()=>{const audio=isSuccess?audioSuccess:audioError;if(audio){audio.currentTime=0;audio.play().catch(e=>console.warn("Autoplay blocked:",e));}});return;}
const audio=isSuccess?audioSuccess:audioError;audio.currentTime=0;audio.play().catch(error=>{console.warn("Браузер заблокировал автовоспроизведение звука:",error);});}
function showCompletionNotification(successItems,failureItems){if(!notifyEnabled||!notificationsSupported||Notification.permission!=='granted')return;const okCount=successItems.length;const failCount=failureItems.length;if(okCount+failCount===0)return;let title,body;if(failCount&&!okCount){title=failCount===1?'Не вышло скачать':`Не вышло: ${failCount} шт.`;body=failCount===1?(failureItems[0].file||''):'';}else if(okCount&&!failCount){title=okCount===1?'Готово, лови':`Готово: ${okCount} шт.`;body=okCount===1?(successItems[0].file||''):'';}else{title='Загрузки завершились';body=`Готово ${okCount}, не вышло ${failCount}`;}
try{const note=new Notification(title,{body:body,icon:'img/snej.webp',tag:'yt-download',silent:true});note.onclick=()=>{window.focus();window.location.hash='#downloads';note.close();};}catch(e){}}
function updateTabTitleProgress(jobs){if(originalDocTitle===null)originalDocTitle=document.title;if(!jobs||jobs.length===0){if(document.title!==originalDocTitle)document.title=originalDocTitle;return;}
let pct=null;for(const job of jobs){const m=/(\d{1,3}(?:\.\d+)?)%/.exec(job.status||'');if(m){pct=Math.min(100,Math.round(parseFloat(m[1])));break;}}
const next=(pct!==null?pct+'% ':'⏳ ')+originalDocTitle;if(document.title!==next)document.title=next;}
function updateNotifyButtonVisuals(btn){const denied=notificationsSupported&&Notification.permission==='denied';btn.innerHTML=notifyEnabled?'🔔':'🔕';if(denied){btn.title='Браузер заблокировал уведомления. Разреши их в настройках сайта у адресной строки.';}else if(notifyEnabled){btn.title='Позову, когда файл будет готов. Нажми, чтобы выключить.';}else{btn.title='Нажми - и позову, когда файл скачается, даже если ты на другой вкладке.';}}
function updateNotifyButtonVisibility(){if(!notifyToggleBtn)return;notifyToggleBtn.classList.toggle('is-visible',isOnHomePage());}
function initNotifyToggle(){if(!notificationsSupported)return;try{notifyEnabled=localStorage.getItem(NOTIFY_KEY)==='true'&&Notification.permission==='granted';}catch(e){notifyEnabled=false;}
const btn=document.createElement('div');btn.id='notify-toggle';btn.className='notify-toggle';notifyToggleBtn=btn;updateNotifyButtonVisuals(btn);updateNotifyButtonVisibility();btn.addEventListener('click',()=>{if(Notification.permission==='granted'){notifyEnabled=!notifyEnabled;try{localStorage.setItem(NOTIFY_KEY,notifyEnabled);}catch(e){}
updateNotifyButtonVisuals(btn);}else if(Notification.permission==='denied'){btn.classList.remove('notify-nudge');void btn.offsetWidth;btn.classList.add('notify-nudge');}else{Notification.requestPermission().then((perm)=>{notifyEnabled=perm==='granted';try{localStorage.setItem(NOTIFY_KEY,notifyEnabled);}catch(e){}
updateNotifyButtonVisuals(btn);});}});document.body.appendChild(btn);}
if(notificationsSupported){if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',()=>{initNotifyToggle();window.addEventListener('hashchange',updateNotifyButtonVisibility);});}else{initNotifyToggle();window.addEventListener('hashchange',updateNotifyButtonVisibility);}}
function getIconClass(type){return type==="audio"?"fa-music":"fa-video-camera";}
const urlsCache=new Map();function renderUrls(urlString,includeIcon=false,iconType=null,leadingBreak=true){if(!urlString)return"";const key=`${urlString}|${includeIcon}|${iconType}|${leadingBreak}`;if(urlsCache.has(key))return urlsCache.get(key);const result=urlString.split(",").filter(url=>url.trim()).map((url,idx)=>{const iconHtml=includeIcon?`<i class="fa ${getIconClass(iconType)}"></i> `:"";const prefix=(idx===0&&!leadingBreak)?"":"<br />";return`${prefix}<a href="${safeUrlAttr(url)}">${iconHtml}${escapeHtml(url)}</a>`;}).join("");urlsCache.set(key,result);return result;}
function computeDataHash(items){if(!items||items.length===0)return'0';let hash='';for(const item of items){for(const key in item){if(item.hasOwnProperty(key)){hash+=item[key];}}
hash+='|';}
return hash;}
function renderTable(container,items,cols,emptyMsg,rowHtmlGenerator,footerHtml=""){const hash=computeDataHash(items)+':'+footerHtml;if(container.dataset.lastHash===hash){return;}
const newHtml=(!items||items.length===0)?`<tr><td colspan="${cols}">${emptyMsg}</td></tr>`:items.map(rowHtmlGenerator).join("")+(footerHtml?`<tr>${footerHtml}</tr>`:"");container.innerHTML=newHtml;container.dataset.lastHash=hash;}
function renderJobRow(job){const iconClass=getIconClass(job.type);const urlsHtml=renderUrls(job.url);return`
    <tr>
        <td style="vertical-align: middle;">${escapeHtml(job.site)}</td>
        <td style="vertical-align: middle;"><i class="fa ${iconClass}"></i> ${escapeHtml(job.file)} ${urlsHtml}</td>
        <td style="vertical-align: middle;">${escapeHtml(job.status)}</td>
        <td style="vertical-align: middle;">
            <div class="btn-group">
                <button type="button" style="width: 100px;" data-action="kill" data-value="${safePid(job.pid)}" class="btn btn-danger btn-xs">Стоп</button>
            </div>
        </td>
    </tr>`;}
function renderQueueRow(item){const iconClass=getIconClass(item.type);const urlsHtml=renderUrls(item.url,true,item.type,false);return`
    <tr>
        <td style="vertical-align: middle;">${urlsHtml}</td>
        <td style="vertical-align: middle;">${escapeHtml(item.dl_format)}</td>
        <td style="vertical-align: middle;">
            <div class="btn-group">
                <button type="button" style="width: 160px" data-action="removeQueued" data-value="${safePid(item.pid)}" class="btn btn-danger btn-xs">Удалить</button>
            </div>
        </td>
    </tr>`;}
function renderFinishedRow(item,logURL){const iconClass=getIconClass(item.type);const urlsHtml=renderUrls(item.url);let logButton="";let actionBtnWidth="80px";if(logURL&&logURL!==""){actionBtnWidth="60px";logButton=`<a href="${safeUrlAttr(logURL)}/${safePid(item.pid)}" style="width: 40px;" target="_blank" class="btn btn-default btn-xs">Лог</a>`;}
return`
    <tr>
        <td style="vertical-align: middle;">${escapeHtml(item.site)}</td>
        <td style="vertical-align: middle;"><i class="fa ${iconClass}"></i> ${escapeHtml(item.file)} ${urlsHtml}</td>
        <td style="vertical-align: middle;">${escapeHtml(item.status)}</td>
        <td style="vertical-align: middle;">
            <div class="btn-group">
                ${logButton}
                <button type="button" style="width: ${actionBtnWidth}" data-action="restart" data-value="${safePid(item.pid)}" class="btn btn-success btn-xs">↺</button>
                <button type="button" style="width: ${actionBtnWidth}" data-action="clear" data-value="${safePid(item.pid)}" class="btn btn-danger btn-xs">Удалить</button>
            </div>
        </td>
    </tr>`;}
function buildFileActions(file){const playIcon=`<svg class="play-btn-icon" viewBox="0 0 24 24" width="14" height="14" fill="currentColor" aria-hidden="true"><path d="M8 5v14l11-7z"/></svg>`;const playButton=file.downloadurl?`<button type="button" class="btn btn-default btn-xs play-btn" title="Глянуть/послушать прямо тут" data-play-url="${escapeHtml(file.downloadurl)}" data-play-kind="${escapeHtml(file.kind || 'video')}">${playIcon}</button>`:'';const qrIcon=`<svg class="qr-btn-icon" viewBox="0 0 24 24" width="14" height="14" fill="currentColor" aria-hidden="true"><path d="M3 3h8v8H3V3zm2 2v4h4V5H5zm-2 8h8v8H3v-8zm2 2v4h4v-4H5zM13 3h8v8h-8V3zm2 2v4h4V5h-4zm-2 8h3v2h-2v3h-2v-5h1zm5 0h3v3h-2v2h-3v-2h2v-3zm-3 5h2v3h-2v-3zm5 0h3v3h-3v-3z"/></svg>`;const qrButton=file.downloadurl?`<button type="button" class="btn btn-default btn-xs qr-btn" title="Забрать на телефон - покажу QR" data-qr-url="${escapeHtml(file.downloadurl)}">${qrIcon}</button>`:'';if(!playButton&&!qrButton&&!file.deleteurl)return'';return`<div class="btn-group btn-group-file">${playButton}${qrButton}${file.deleteurl}</div>`;}
function getFileKey(file){return file.downloadurl||file.file;}
function renderFileRow(file,isNew){const actions=buildFileActions(file);const enterClass=isNew?' row-enter-cell':'';if(typeof showFileLifetime!=='undefined'&&!showFileLifetime){return`<tr><td><span class="file-name-plain${enterClass}">${file.file}</span></td><td>${file.size}</td><td>${actions}</td></tr>`;}
const age=(typeof file.age_minutes==='number'&&!isNaN(file.age_minutes))?file.age_minutes:0;const percent=(typeof file.lifetime_percent==='number'&&!isNaN(file.lifetime_percent))?file.lifetime_percent:100;let timeText='';let colorClass='bg-safe';const retention=(typeof retentionMinutes!=='undefined'&&retentionMinutes>0)?retentionMinutes:120;const remainingMinutes=Math.max(0,retention-age);if(percent>60)colorClass='bg-safe';else if(percent>30)colorClass='bg-warn';else if(percent>0)colorClass='bg-danger';else colorClass='bg-dead';if(remainingMinutes>60){const hours=Math.floor(remainingMinutes/60);timeText=`${hours}ч ${remainingMinutes % 60}м`;}else if(remainingMinutes>0){timeText=`${remainingMinutes}м`;}else{timeText='скоро';}
const badge=`
        <div class="lifetime-badge">
            <div class="lifetime-badge-text">⏱ ${timeText}</div>
            <div class="progress">
                <div class="progress-bar progress-lifetime ${colorClass}" style="width: ${percent}%;"></div>
            </div>
        </div>`;return`
        <tr>
            <td>
                <div class="file-row-content${enterClass}">
                    ${badge}
                    <span class="file-name">${file.file}</span>
                </div>
            </td>
            <td>${file.size}</td>
            <td>${actions}</td>
        </tr>`;}
function buildAbsoluteFileUrl(relativeUrl){try{return new URL(relativeUrl,window.location.href).href;}catch(e){return null;}}
let qrModalEl=null;let qrLibPromise=null;function ensureQrLib(){if(typeof qrcode!=='undefined')return Promise.resolve();if(qrLibPromise)return qrLibPromise;qrLibPromise=new Promise((resolve,reject)=>{const script=document.createElement('script');script.src='js/qrcode.min.js';script.async=true;script.onload=()=>resolve();script.onerror=()=>{qrLibPromise=null;reject(new Error('Не удалось загрузить библиотеку QR'));};document.head.appendChild(script);});return qrLibPromise;}
function ensureQrModal(){if(qrModalEl)return qrModalEl;const overlay=document.createElement('div');overlay.className='qr-modal-overlay';overlay.innerHTML=`
        <div class="qr-modal-card" role="dialog" aria-modal="true" aria-label="QR-код, чтобы забрать файл на телефон">
            <button type="button" class="qr-modal-close" aria-label="Закрыть">&times;</button>
            <div class="qr-modal-title">Лови на телефон</div>
            <div class="qr-modal-subtitle">Наведи камеру - и файл уже у тебя</div>
            <div class="qr-modal-canvas-wrap">
                <canvas class="qr-modal-canvas" width="320" height="320"></canvas>
                <div class="qr-modal-bird"><img class="qr-modal-bird-img" src="img/snej.webp" alt="" draggable="false"></div>
            </div>
            <div class="qr-modal-filename"></div>
            <div class="qr-modal-hint">Снегирь покараулит ссылку, не торопись</div>
            <a class="qr-modal-link" href="#" target="_blank" rel="noopener">Открыть прямо здесь</a>
        </div>`;document.body.appendChild(overlay);qrModalEl=overlay;overlay.addEventListener('click',(e)=>{if(e.target===overlay)hideQrModal();});overlay.querySelector('.qr-modal-close').addEventListener('click',hideQrModal);document.addEventListener('keydown',(e)=>{if(e.key==='Escape'&&overlay.classList.contains('is-visible'))hideQrModal();});return overlay;}
function drawQrToCanvas(canvas,text){if(typeof qrcode==='undefined')throw new Error('Библиотека QR не загружена');const qr=qrcode(0,'H');qr.addData(text);qr.make();const count=qr.getModuleCount();const size=canvas.width;const margin=4;const cell=size/(count+margin*2);const ctx=canvas.getContext('2d');ctx.fillStyle='#ffffff';ctx.fillRect(0,0,size,size);ctx.fillStyle='#1c1c1c';for(let r=0;r<count;r++){for(let c=0;c<count;c++){if(qr.isDark(r,c)){const x=(c+margin)*cell;const y=(r+margin)*cell;ctx.fillRect(Math.floor(x),Math.floor(y),Math.ceil(cell),Math.ceil(cell));}}}}
function showQrModal(relativeUrl){const abs=buildAbsoluteFileUrl(relativeUrl);if(!abs)return;const overlay=ensureQrModal();let name=relativeUrl;try{name=decodeURIComponent(relativeUrl.split('/').pop());}catch(e){}
overlay.querySelector('.qr-modal-filename').textContent=name;overlay.querySelector('.qr-modal-link').href=abs;overlay.classList.add('is-visible');const canvas=overlay.querySelector('.qr-modal-canvas');ensureQrLib().then(()=>drawQrToCanvas(canvas,abs)).catch((e)=>console.error('Не удалось построить QR:',e));}
function hideQrModal(){if(qrModalEl)qrModalEl.classList.remove('is-visible');}
document.addEventListener('click',function(e){const btn=e.target.closest('.qr-btn');if(!btn)return;e.preventDefault();const url=btn.getAttribute('data-qr-url');if(url)showQrModal(url);});let playerModalEl=null;function ensurePlayerModal(){if(playerModalEl)return playerModalEl;const overlay=document.createElement('div');overlay.className='qr-modal-overlay player-modal-overlay';overlay.innerHTML=`
        <div class="qr-modal-card player-modal-card" role="dialog" aria-modal="true" aria-label="Просмотр файла">
            <button type="button" class="qr-modal-close" aria-label="Закрыть">&times;</button>
            <div class="qr-modal-title player-modal-title"></div>
            <div class="player-modal-video-wrap is-hidden">
                <video class="player-modal-video" controls playsinline preload="metadata"></video>
            </div>
            <div class="player-modal-audio-wrap is-hidden">
                <audio class="player-modal-audio" controls preload="metadata"></audio>
            </div>
            <div class="qr-modal-filename player-modal-filename"></div>
        </div>`;document.body.appendChild(overlay);playerModalEl=overlay;overlay.addEventListener('click',(e)=>{if(e.target===overlay)hidePlayerModal();});overlay.querySelector('.qr-modal-close').addEventListener('click',hidePlayerModal);document.addEventListener('keydown',(e)=>{if(e.key==='Escape'&&overlay.classList.contains('is-visible'))hidePlayerModal();});return overlay;}
function showPlayerModal(relativeUrl,kind){const abs=buildAbsoluteFileUrl(relativeUrl);if(!abs)return;const overlay=ensurePlayerModal();const videoWrap=overlay.querySelector('.player-modal-video-wrap');const audioWrap=overlay.querySelector('.player-modal-audio-wrap');const video=overlay.querySelector('.player-modal-video');const audio=overlay.querySelector('.player-modal-audio');const isAudio=kind==='audio';overlay.querySelector('.player-modal-title').textContent=isAudio?'Слушаем':'Смотрим';let name=relativeUrl;try{name=decodeURIComponent(relativeUrl.split('/').pop());}catch(e){}
overlay.querySelector('.player-modal-filename').textContent=name;videoWrap.classList.toggle('is-hidden',isAudio);audioWrap.classList.toggle('is-hidden',!isAudio);if(isAudio){video.pause();video.removeAttribute('src');video.load();audio.src=abs;}else{audio.pause();audio.removeAttribute('src');audio.load();video.src=abs;}
overlay.classList.add('is-visible');}
function hidePlayerModal(){if(!playerModalEl)return;playerModalEl.classList.remove('is-visible');const video=playerModalEl.querySelector('.player-modal-video');const audio=playerModalEl.querySelector('.player-modal-audio');video.pause();video.removeAttribute('src');video.load();audio.pause();audio.removeAttribute('src');audio.load();}
document.addEventListener('click',function(e){const btn=e.target.closest('.play-btn');if(!btn)return;e.preventDefault();const url=btn.getAttribute('data-play-url');const kind=btn.getAttribute('data-play-kind');if(url)showPlayerModal(url,kind);});document.addEventListener('click',function(e){const act=e.target.closest('[data-action]');if(act){e.preventDefault();const extra={};const type=act.getAttribute('data-type');if(type)extra.type=type;confirmAction(act.getAttribute('data-action'),act.getAttribute('data-value')||'',extra);return;}
if(e.target.closest('[data-ui="help"]')){helpPanel();return;}
const goto=e.target.closest('[data-goto]');if(goto){e.preventDefault();const nav=document.getElementById(goto.getAttribute('data-goto'));if(nav)nav.click();return;}});document.addEventListener('change',function(e){const t=e.target;if(!t)return;if(t.id==='ui_audio_mode'||t.id==='ui_quality_toggle'){syncLogic();}else if(t.classList&&t.classList.contains('toggle-input-sub')){syncSubToggles(t);}});function loadList(){fetch("index.php?jobs").then(resp=>resp.json()).then(function(data){const currentFinishedPids=new Set();for(const item of data.finished){currentFinishedPids.add(String(item.pid));}
if(previousFinishedPids!==null){const newSuccess=[];const newFailure=[];for(let item of data.finished){const pid=String(item.pid);if(!previousFinishedPids.has(pid)){(isDownloadFailed(item)?newFailure:newSuccess).push(item);}}
if(newFailure.length){playNotificationSound(false);}else if(newSuccess.length){playNotificationSound(true);}
if(document.hidden&&(newSuccess.length||newFailure.length)){showCompletionNotification(newSuccess,newFailure);}}
previousFinishedPids=currentFinishedPids;renderTable(nativeUI.progress,data.jobs,4,"Активных загрузок нет.",renderJobRow,`
            <td></td><td></td><td></td>
            <td><div class="btn-group"><button type="button" id="killallbutton" style="width: 100px;" class="btn btn-danger btn-xs" data-action="kill" data-value="all">Стоп ВСЕ</button></div></td>`);renderTable(nativeUI.queue,data.queue,3,"Очередь пуста.",renderQueueRow,`
            <td></td><td></td>
            <td><div class="btn-group"><button type="button" id="clearallbutton-queue" style="width: 160px;" class="btn btn-danger btn-xs" data-action="clear" data-value="queue">Удалить Все</button></div></td>`);renderTable(nativeUI.completed,data.finished,4,"Завершенных загрузок нет.",item=>renderFinishedRow(item,data.logURL),`
            <td></td><td></td><td></td>
            <td><div class="btn-group"><button type="button" id="clearallbutton-finished" style="width: 160px;" class="btn btn-danger btn-xs" data-action="clear" data-value="recent">Удалить Все</button></div></td>`);const currentVideoKeys=new Set((data.videos||[]).map(getFileKey));const newVideoKeys=previousVideoKeys===null?new Set():new Set([...currentVideoKeys].filter(k=>!previousVideoKeys.has(k)));previousVideoKeys=currentVideoKeys;const currentMusicKeys=new Set((data.music||[]).map(getFileKey));const newMusicKeys=previousMusicKeys===null?new Set():new Set([...currentMusicKeys].filter(k=>!previousMusicKeys.has(k)));previousMusicKeys=currentMusicKeys;renderTable(nativeUI.videos,data.videos,3,"Видео нет.",item=>renderFileRow(item,newVideoKeys.has(getFileKey(item))));renderTable(nativeUI.music,data.music,3,"Музыки нет.",item=>renderFileRow(item,newMusicKeys.has(getFileKey(item))));updateFileBadges(data);updateProxyStatus(data.proxy);updateTabTitleProgress(data.jobs);const isActive=(data.jobs&&data.jobs.length>0)||(data.queue&&data.queue.length>0);lastActiveState=isActive;if(document.hidden&&!isActive){stopAutoRefresh();return;}
scheduleNextRefresh(document.hidden?BG_POLL_INTERVAL:(isActive?CONFIG.fastInterval:CONFIG.slowInterval));}).catch(function(){console.error("Не удалось загрузить данные");scheduleNextRefresh(CONFIG.slowInterval);});}
let refreshTimer=null;let refreshActive=false;let urlInput=null;function initCache(){nativeUI.progress=document.getElementById('dlprogress');nativeUI.queue=document.getElementById('dlqueue');nativeUI.completed=document.getElementById('dlcompleted');nativeUI.videos=document.getElementById('videofiles');nativeUI.music=document.getElementById('musicfiles');urlInput=document.getElementById('url');}
function scheduleNextRefresh(delay){clearTimeout(refreshTimer);if(!refreshActive)return;refreshTimer=setTimeout(loadList,delay);}
function startAutoRefresh(){if(nativeUI.progress&&!refreshActive){refreshActive=true;loadList();}}
function stopAutoRefresh(){refreshActive=false;clearTimeout(refreshTimer);refreshTimer=null;}
document.addEventListener('DOMContentLoaded',function(){initCache();if(nativeUI.progress){startAutoRefresh();if(urlInput)urlInput.focus();}
document.addEventListener('submit',function(e){const form=e.target;if(form.tagName!=='FORM')return;if(!form.querySelector('input[name="csrf_token"]')){const input=document.createElement('input');input.type='hidden';input.name='csrf_token';input.value=getCsrfToken();form.appendChild(input);}});});window.addEventListener('beforeunload',function(){stopAutoRefresh();});document.addEventListener('visibilitychange',function(){if(document.hidden){if(notifyEnabled&&lastActiveState&&notificationsSupported&&Notification.permission==='granted'){scheduleNextRefresh(BG_POLL_INTERVAL);}else{stopAutoRefresh();}}else{if(nativeUI.progress){if(!refreshActive){startAutoRefresh();}else{clearTimeout(refreshTimer);loadList();}}}});function helpPanel(){const panelBody=document.getElementById('helppanel');const helpLink=document.getElementById('helplink');if(!panelBody.classList.contains('panel-collapsed')){panelBody.classList.add('panel-collapsed');helpLink.innerHTML='Я туть, твоя помощь';}else{panelBody.classList.remove('panel-collapsed');helpLink.innerHTML='Скрыть';}}
document.addEventListener('DOMContentLoaded',function(){const faviconContainer=document.getElementById('url-favicon');const faviconImg=document.getElementById('url-favicon-img');const clearBtn=document.getElementById('url-clear');const urlInput=document.getElementById('url');const wrapper=document.querySelector('.url-input-wrapper');let inputTimer=null;const INPUT_DELAY=150;let isClearing=false;const FAVICON_BASE='favicons/';const FALLBACK_ICON='data:image/svg+xml;base64,';const faviconCache=new Map();const KNOWN_SERVICES_SET=new Set(KNOWN_SERVICES);const serviceIndex=new Map();for(const service of KNOWN_SERVICES){const parts=service.split('.');const key=parts.slice(-2).join('.');if(!serviceIndex.has(key))serviceIndex.set(key,[]);serviceIndex.get(key).push(service);}
function getBaseService(hostname){if(!hostname)return null;hostname=hostname.toLowerCase();if(KNOWN_SERVICES_SET.has(hostname))return hostname;const parts=hostname.split('.');for(let i=1;i<parts.length-1;i++){const key=parts.slice(i).join('.');const candidates=serviceIndex.get(key);if(candidates){for(const service of candidates){if(hostname===service||hostname.endsWith('.'+service)){return service;}}}}
return null;}
function getFaviconUrl(domain){return`${FAVICON_BASE}${encodeURIComponent(domain)}.png`;}
function applyFavicon(url){if(faviconImg.getAttribute('src')!==url){faviconImg.setAttribute('src',url);}
faviconContainer.classList.add('is-visible');wrapper.classList.add('has-favicon');}
function resetUI(){faviconImg.setAttribute('src','');faviconContainer.classList.remove('is-visible');wrapper.classList.remove('has-favicon');if(urlInput.value.trim()&&!isClearing){clearBtn.classList.add('is-visible');}}
function showFavicon(serviceDomain){if(!urlInput.value.trim()||isClearing)return;const cached=faviconCache.get(serviceDomain);if(cached){if(cached.ok){applyFavicon(cached.url);}else{resetUI();}
return;}
faviconContainer.classList.remove('is-visible');wrapper.classList.remove('has-favicon');const url=getFaviconUrl(serviceDomain);const tempImg=new Image();tempImg.onload=function(){if(!urlInput.value.trim()||isClearing)return;faviconCache.set(serviceDomain,{url,ok:true});const currentService=getBaseService((()=>{try{let v=urlInput.value.trim().split('||')[0].trim();if(!/^https?:\/\//i.test(v))v='https://'+v;return new URL(v).hostname.replace(/^www\./i,'');}catch(e){return null;}})());if(currentService!==serviceDomain)return;applyFavicon(url);};tempImg.onerror=function(){if(!urlInput.value.trim()||isClearing)return;faviconCache.set(serviceDomain,{url:FALLBACK_ICON,ok:false});resetUI();};tempImg.src=url;}
function hideFavicon(){faviconImg.onload=null;faviconImg.onerror=null;faviconContainer.classList.remove('is-visible');wrapper.classList.remove('has-favicon');}
function showClearBtn(){clearBtn.classList.add('is-visible');}
function hideClearBtn(){clearBtn.classList.remove('is-visible');}
function clearInput(){isClearing=true;faviconImg.onload=null;faviconImg.onerror=null;clearTimeout(inputTimer);urlInput.value='';faviconContainer.classList.remove('is-visible');wrapper.classList.remove('has-favicon');clearBtn.classList.remove('is-visible');urlInput.focus();setTimeout(()=>{isClearing=false;},50);}
function checkUrl(){if(isClearing)return;const val=urlInput.value.trim();if(!val){hideFavicon();hideClearBtn();return;}
const firstUrl=val.split('||')[0].trim();let hostname=null;try{let urlToParse=firstUrl;if(!/^https?:\/\//i.test(urlToParse))urlToParse='https://'+urlToParse;hostname=new URL(urlToParse).hostname.replace(/^www\./i,'');}catch(e){hostname=null;}
const service=hostname?getBaseService(hostname):null;if(service){hideClearBtn();showFavicon(service);}else{hideFavicon();showClearBtn();}}
urlInput.addEventListener('paste',()=>setTimeout(checkUrl,10));urlInput.addEventListener('input',()=>{clearTimeout(inputTimer);inputTimer=setTimeout(checkUrl,INPUT_DELAY);});urlInput.addEventListener('blur',()=>{if(isClearing)return;clearTimeout(inputTimer);checkUrl();});['mousedown','touchstart'].forEach(evt=>{clearBtn.addEventListener(evt,(e)=>{e.preventDefault();clearInput();});faviconContainer.addEventListener(evt,(e)=>{e.preventDefault();clearInput();});});if(urlInput.value.trim())setTimeout(checkUrl,50);const CLIPBOARD_MAGIC_KEY='clipboardMagicEnabled';const CLIPBOARD_MAGIC_DISMISSED_KEY='clipboardMagicDismissed';let lastClipboardText=null;function playSnejWink(){const snejDiv=document.getElementById('snej');if(!snejDiv)return;snejDiv.classList.remove('snej-wink');void snejDiv.offsetWidth;snejDiv.classList.add('snej-wink');setTimeout(()=>snejDiv.classList.remove('snej-wink'),500);}
function isClipboardMagicEnabled(){try{return localStorage.getItem(CLIPBOARD_MAGIC_KEY)==='1';}catch(e){return false;}}
function matchKnownServiceUrl(text){const trimmed=(text||'').trim();if(!trimmed)return null;let hostname=null;try{let urlToParse=trimmed;if(!/^https?:\/\//i.test(urlToParse))urlToParse='https://'+urlToParse;hostname=new URL(urlToParse).hostname.replace(/^www\./i,'');}catch(e){return null;}
const service=hostname?getBaseService(hostname):null;return service?trimmed:null;}
function applyClipboardText(trimmed){urlInput.value=trimmed;checkUrl();playSnejWink();}
function tryClipboardMagic(){if(!isClipboardMagicEnabled())return;if(isClearing||!document.hasFocus())return;if(urlInput.value.trim())return;if(!navigator.clipboard||!navigator.clipboard.readText)return;navigator.clipboard.readText().then((text)=>{const trimmed=(text||'').trim();if(!trimmed||trimmed===lastClipboardText)return;lastClipboardText=trimmed;if(urlInput.value.trim()||isClearing)return;const matched=matchKnownServiceUrl(trimmed);if(!matched)return;applyClipboardText(matched);}).catch(()=>{});}
document.addEventListener('visibilitychange',()=>{if(!document.hidden)tryClipboardMagic();});window.addEventListener('focus',tryClipboardMagic);const magicPrompt=document.getElementById('clipboard-magic-prompt');const magicYesBtn=document.getElementById('clipboard-magic-yes');const magicNoBtn=document.getElementById('clipboard-magic-no');function clipboardMagicDecided(){try{return localStorage.getItem(CLIPBOARD_MAGIC_KEY)!==null||localStorage.getItem(CLIPBOARD_MAGIC_DISMISSED_KEY)==='1';}catch(e){return true;}}
if(magicPrompt&&magicYesBtn&&magicNoBtn&&navigator.clipboard&&navigator.clipboard.readText&&!clipboardMagicDecided()){magicPrompt.classList.remove('is-hidden');magicYesBtn.addEventListener('click',()=>{navigator.clipboard.readText().then(()=>{try{localStorage.setItem(CLIPBOARD_MAGIC_KEY,'1');}catch(e){}
magicPrompt.classList.add('is-hidden');tryClipboardMagic();}).catch(()=>{magicPrompt.classList.add('is-hidden');});});magicNoBtn.addEventListener('click',()=>{try{localStorage.setItem(CLIPBOARD_MAGIC_DISMISSED_KEY,'1');}catch(e){}
magicPrompt.classList.add('is-hidden');});}});function syncLogic(){const isAudio=document.getElementById('ui_audio_mode').checked;const paramsVideo=document.getElementById('params-video');const paramsAudio=document.getElementById('params-audio');const hiddenAudioCheckbox=document.getElementById('audio_convert');const hiddenVideoFormat=document.getElementById('format');const qualityToggle=document.getElementById('ui_quality_toggle');paramsVideo.classList.toggle('is-hidden',isAudio);paramsAudio.classList.toggle('is-hidden',!isAudio);hiddenAudioCheckbox.checked=isAudio;if(!isAudio){hiddenVideoFormat.value=qualityToggle.checked?'worst':'top';}
if(isAudio)syncHiddenSelects();}
function syncSubToggles(clickedToggle){if(!clickedToggle.checked){clickedToggle.checked=true;return;}
const parentGroup=clickedToggle.closest('.audio-switches-wrapper');parentGroup.querySelectorAll('.toggle-input-sub').forEach(toggle=>{if(toggle!==clickedToggle)toggle.checked=false;});syncHiddenSelects();}
function syncHiddenSelects(){const hiddenAudioFormat=document.getElementById('audio_format');const activeToggle=document.querySelector('#params-audio .toggle-input-sub:checked');if(activeToggle)hiddenAudioFormat.value=activeToggle.getAttribute('data-value');}
function initLongPressQualitySelector(){const downloadBtn=document.querySelector('.btn-download-minimal');const qualityPopup=document.getElementById('quality-popup');const downloadForm=document.getElementById('download-form');const formatField=document.getElementById('format');const uiAudioMode=document.getElementById('ui_audio_mode');const controlsRow=document.querySelector('.controls-row');if(!downloadBtn||!qualityPopup)return;const ALLOWED_FORMATS=new Set(['4K','1440p','1080p']);const LONG_PRESS_TIME=500;let longPressTimer=null;let isLongPress=false;let isPointerDown=false;let selectedMenuItem=null;downloadBtn.addEventListener('pointerdown',(e)=>{const qualityToggle=document.getElementById('ui_quality_toggle');if(uiAudioMode.checked||qualityToggle.checked)return;isPointerDown=true;isLongPress=false;selectedMenuItem=null;longPressTimer=setTimeout(()=>{if(isPointerDown){isLongPress=true;downloadBtn.classList.add('is-pressed');showQualityMenu();}},LONG_PRESS_TIME);});document.addEventListener('pointermove',(e)=>{if(!isLongPress||!qualityPopup.classList.contains('is-visible'))return;const menuItems=qualityPopup.querySelectorAll('.quality-popup-item');let hoveredItem=null;for(const item of menuItems){const rect=item.getBoundingClientRect();if(e.clientX>=rect.left&&e.clientX<=rect.right&&e.clientY>=rect.top&&e.clientY<=rect.bottom){hoveredItem=item;break;}}
menuItems.forEach(item=>{item.classList.toggle('is-selected',item===hoveredItem);});selectedMenuItem=hoveredItem;});document.addEventListener('pointerup',(e)=>{clearTimeout(longPressTimer);isPointerDown=false;downloadBtn.classList.remove('is-pressed');if(isLongPress&&selectedMenuItem){const format=selectedMenuItem.getAttribute('data-format');if(ALLOWED_FORMATS.has(format)){formatField.value=format;hideQualityMenu();downloadForm.requestSubmit();}else{console.warn('Invalid format selected:',format);hideQualityMenu();}}else{hideQualityMenu();}
isLongPress=false;selectedMenuItem=null;});document.addEventListener('keydown',(e)=>{if(e.key==='Escape'&&qualityPopup.classList.contains('is-visible')){hideQualityMenu();isLongPress=false;selectedMenuItem=null;}});function showQualityMenu(){if(controlsRow)controlsRow.classList.add('is-controls-hidden');qualityPopup.classList.add('is-visible');if(navigator.vibrate){navigator.vibrate(50);}}
function hideQualityMenu(){if(controlsRow)controlsRow.classList.remove('is-controls-hidden');qualityPopup.classList.remove('is-visible');const items=qualityPopup.querySelectorAll('.quality-popup-item');items.forEach(item=>item.classList.remove('is-selected'));}}
document.addEventListener('DOMContentLoaded',function(){const hiddenAudioCheckbox=document.getElementById('audio_convert');const hiddenAudioFormat=document.getElementById('audio_format');const hiddenVideoFormat=document.getElementById('format');const uiAudioMode=document.getElementById('ui_audio_mode');const qualityToggle=document.getElementById('ui_quality_toggle');uiAudioMode.checked=hiddenAudioCheckbox.checked;qualityToggle.checked=(hiddenVideoFormat.value==='worst');syncLogic();if(uiAudioMode.checked&&hiddenAudioFormat){const targetToggle=document.querySelector(`#params-audio .toggle-input-sub[data-value="${hiddenAudioFormat.value}"]`);if(targetToggle){targetToggle.checked=true;document.querySelectorAll('#params-audio .toggle-input-sub').forEach(t=>{if(t!==targetToggle)t.checked=false;});}}
initSnejEasterEgg();initLongPressQualitySelector();});const SNEJ_CLICKS_TO_FIRE=30;const SNEJ_RESET_DELAY_START=420;const SNEJ_RESET_DELAY_END=180;const SNEJ_MAX_SHAKE_LEVEL=SNEJ_CLICKS_TO_FIRE-2;const SNEJ_GLOW_START_CLICK=8;const SNEJ_MAX_GLOW_LEVEL=SNEJ_CLICKS_TO_FIRE-SNEJ_GLOW_START_CLICK;const SNEJ_RARE_CHANCE=0.12;const SNEJ_RARE_VARIANTS=['snej-laser-rare-purple','snej-laser-rare-green','snej-laser-rare-blue','snej-laser-rare-gold','snej-laser-rare-white'];function initSnejEasterEgg(){let clickCount=0;let resetTimer=null;const snejDiv=document.querySelector('#snej');const snejInput=snejDiv?snejDiv.querySelector('input[type="image"]'):null;const snejWrap=snejDiv?snejDiv.querySelector('.snej-eye-wrap'):null;const snejGlow=snejDiv?snejDiv.querySelector('.snej-eye-glow'):null;const snejHitArea=snejDiv?snejDiv.querySelector('.snej-hit-area'):null;if(!snejInput||!snejWrap||!snejGlow||!snejHitArea)return;snejInput.addEventListener('dragstart',(e)=>e.preventDefault());snejHitArea.addEventListener('click',(e)=>{e.preventDefault();if(snejDiv.classList.contains('snej-laser-active'))return;clickCount++;clearTimeout(resetTimer);if(clickCount>=SNEJ_CLICKS_TO_FIRE){clickCount=0;resetSnejShake(snejWrap);resetSnejEyeFlash(snejGlow);fireSnejLaser(snejDiv);return;}
if(clickCount>1){applySnejShake(snejWrap,clickCount-1);}
if(clickCount>=SNEJ_GLOW_START_CLICK){applySnejEyeFlash(snejGlow,clickCount-SNEJ_GLOW_START_CLICK+1);}
resetTimer=setTimeout(()=>{clickCount=0;resetSnejShake(snejWrap);},getSnejResetDelay(clickCount));});}
function getSnejResetDelay(clickCount){const progress=Math.min(clickCount/SNEJ_CLICKS_TO_FIRE,1);return SNEJ_RESET_DELAY_START-progress*(SNEJ_RESET_DELAY_START-SNEJ_RESET_DELAY_END);}
function applySnejShake(snejWrap,level){const progress=Math.min(level/SNEJ_MAX_SHAKE_LEVEL,1);const amplitude=2+progress*10;const duration=Math.max(0.45-progress*0.28,0.17);snejWrap.style.setProperty('--shake-amp',amplitude+'px');snejWrap.style.animation='none';void snejWrap.offsetWidth;snejWrap.style.animation=`snej-shake-burst ${duration}s ease-out`;}
function resetSnejShake(snejWrap){snejWrap.style.animation='';snejWrap.style.removeProperty('--shake-amp');}
function applySnejEyeFlash(snejGlow,level){const progress=Math.min(level/SNEJ_MAX_GLOW_LEVEL,1);const peak=0.4+progress*0.6;snejGlow.style.setProperty('--glow-peak',peak);snejGlow.style.animation='none';void snejGlow.offsetWidth;snejGlow.style.animation='snej-eye-pulse-decay 0.5s ease-out';}
function resetSnejEyeFlash(snejGlow){snejGlow.style.animation='';snejGlow.style.removeProperty('--glow-peak');}
function fireSnejLaser(snejDiv){const isRare=Math.random()<SNEJ_RARE_CHANCE;const variant=isRare?SNEJ_RARE_VARIANTS[Math.floor(Math.random()*SNEJ_RARE_VARIANTS.length)]:null;if(variant)snejDiv.classList.add(variant);snejDiv.classList.add('snej-laser-active');setTimeout(()=>{snejDiv.classList.remove('snej-laser-active');if(variant)snejDiv.classList.remove(variant);},950);}
